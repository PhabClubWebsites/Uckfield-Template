<%= render partial: "page-header", locals: { bg_img: club.bg_image, title: "Latest News", color: "#3D3976" } %>

<div class="container-fluid pipe-container" id="container_news">
    <div class="container">
        <div class="row">
            <div class="col-md-8 col-sm-12 col-xs-12">
                <div class="outer-well">
                    <% if !@news_pages.blank? %>
                        <% @news_pages.each_with_index do |news, i| %>
                            <%= render "article_preview", obj: news %>
                        <% end %>
                        <div class="apple_pagination">
                          <%= will_paginate @news_pages, :container => false, :previous_label => prev_page_button, :next_label => next_page_button  %>
                        </div>
                    <% end %>
                </div>
            </div>
            <div class="col-md-4 col-sm-12 col-xs-12">
                <%= render 'layouts/page_sidebar' %>
            </div>
        </div>
    </div>
</div>
